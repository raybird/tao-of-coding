[default]
role=oracle
skill=brainstorming
reason=fallback

[route.ideation]
reason=ideation
role=oracle
skill=brainstorming
pattern=發想
pattern=澄清
pattern=方案比較
pattern=brainstorm
pattern=trade[-[:space:]]?off
pattern=需求

[route.planning]
reason=planning
role=oracle
skill=writing-plans
pattern=實作計畫
pattern=implementation[[:space:]]+plan
pattern=寫計畫
pattern=plan[[:space:]]+first
pattern=先規劃

[route.plan-execution]
reason=plan-execution
role=explorer
skill=executing-plans
pattern=執行計畫
pattern=按計畫執行
pattern=按計畫批次執行
pattern=批次執行
pattern=execute[[:space:]]+plan
pattern=batch[[:space:]]+execute

[route.tdd]
reason=tdd
role=fixer
skill=test-driven-development
pattern=tdd
pattern=red-green
pattern=測試先行
pattern=先寫測試
pattern=failing[[:space:]]+test

[route.debugging]
reason=debugging
role=fixer
skill=systematic-debugging
pattern=根因
pattern=除錯
pattern=debug
pattern=unexpected
pattern=失敗
pattern=錯誤
pattern=flaky
pattern=故障

[route.verification]
reason=verification
role=fixer
skill=verification-before-completion
pattern=完成前驗證
pattern=宣告完成
pattern=verification
pattern=evidence[[:space:]]+before[[:space:]]+claims
pattern=verify[[:space:]]+before

[route.requesting-review]
reason=request-review
role=librarian
skill=requesting-code-review
pattern=請求審查
pattern=送審
pattern=request[[:space:]]+review
pattern=pre-review
pattern=pr[[:space:]]+summary
pattern=review[[:space:]]+request

[route.receiving-review]
reason=receive-review
role=fixer
skill=receiving-code-review
pattern=回覆審查
pattern=處理審查
pattern=review[[:space:]]+feedback
pattern=review[[:space:]]+comment
pattern=address[[:space:]]+review
